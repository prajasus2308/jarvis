import { useEffect, useRef } from 'react';
import type { HandFrame } from '../services/visionService';
export default function HandOverlay({frame}:{frame:HandFrame|null}){const ref=useRef<HTMLCanvasElement>(null);useEffect(()=>{const c=ref.current;if(!c)return;const x=c.getContext('2d')!, r=c.getBoundingClientRect();c.width=r.width;c.height=r.height;x.clearRect(0,0,c.width,c.height);if(!frame)return;x.strokeStyle='#38e8ff';x.fillStyle='#d9fbff';const chains=[[0,1,2,3,4],[0,5,6,7,8],[0,9,10,11,12],[0,13,14,15,16],[0,17,18,19,20]];for(const chain of chains){x.beginPath();chain.forEach((i,n)=>{const p=frame.landmarks[i];n?x.lineTo((1-p.x)*c.width,p.y*c.height):x.moveTo((1-p.x)*c.width,p.y*c.height)});x.stroke()}for(const p of frame.landmarks){x.beginPath();x.arc((1-p.x)*c.width,p.y*c.height,3,0,Math.PI*2);x.fill()}},[frame]);return <canvas className="hand-overlay" ref={ref}/>}
